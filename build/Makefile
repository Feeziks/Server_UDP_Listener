#Make file that is probably bad
#Compiler Program
CC = g++

#Compiler Flags:
#       -Wall : turn on compiler warnings

#Build Target
TARGET = listener

all: $(TARGET)

$(TARGET):
	$(CC) $(CFLAGS) -o $(TARGET) ../src/*

clean:
	rm $(TARGET)
